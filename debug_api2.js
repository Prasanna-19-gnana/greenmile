const { POST } = require('./.next/server/app/api/trips/compare/route.js'); // Cannot easily run built next.js files

// Let's just create a mock request object and invoke the function directly inside a script, using ts-node
